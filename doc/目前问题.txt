性能
    1.统计的二层map查找，性能降低很严重。。（1700次到1100次）
    2.TCP长连接，当appactor不会产生主动socket时，是7000次/s，发起的话就只有3000次

已经确定的:
    1.【已经修正】timer每次获取gettimeofday对性能影响很小（但是确实会有影响，6800次->6400次这样的变化，当然如果真要优化，也可以一次for循环里只取一次时间，不过这样时间就不太准了）
